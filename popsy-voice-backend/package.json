{
  "name": "popsy-voice-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Backend/API proxy seguro para el asistente de voz de Marcaciones Popsy (OpenAI TTS, gpt-4o-mini-tts). Guarda OPENAI_API_KEY exclusivamente del lado del servidor — nunca la expone al navegador.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
